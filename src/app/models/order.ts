export interface order {
    key: string,
    serie: number,
    initialDate: Date,
    endDate: Date,
    country: string,
    productName: string,
    price: number,
    quantity: number,
    deliveryPrice: number   
}